library(shiny)

shinyUI(fluidPage(
  
  titlePanel("EmptyPlot"),
  
  # sidebarLayout(
  #sidebarPanel(
  # selectInput("name", label = "Name", choices = unique(data$Name))
  #),
  
  #mainPanel(
  #plotOutput("yearplot"),
  #dataTableOutput("yeartable")
  #)
  #)
))